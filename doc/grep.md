grep经常用到，但是却不是很了解一些常用的参数，记录于此。
```
grep -v
    只显示不匹配内容

grep -s
    不输出错误等。

grep -A NUM

    输出匹配文本开始的NUM行

grep -B NUM

    输出匹配文本开始的前面NUM行

grep -C NUM

    输出匹配文本开始的前面NUM和后面的NUM行

grep -i 
    忽略大小写

grep -o 
    只输出匹配内容

grep -n 
    输出匹配内容并打印行号
```